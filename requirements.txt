# Core
psycopg[binary]>=3.3.4
pgvector>=0.4.2
numpy

# Google APIs
google-api-python-client
google-auth-oauthlib
google-auth-httplib2
httplib2

# Embedding
sentence-transformers>=5.5.1
# torch: pip resolves to the CPU wheel by default. For NVIDIA GPU
# (reranker latency 3-13s -> ~200ms, 15x faster search; ~25% faster
# build), uninstall the CPU wheel and install the CUDA variant via
# PyTorch's index AFTER `pip install -r requirements.txt`:
#   pip uninstall torch -y
#   pip install torch==2.6.0 --index-url https://download.pytorch.org/whl/cu124
# Tested on RTX 4070 SUPER + driver 595.79 (CUDA 13.2 runtime). CUDA 12.4
# wheels are forward-compatible with newer drivers.
torch

# Text extraction
pypdf

# API & Web
fastapi
uvicorn[standard]
websockets
jinja2
python-multipart

# MCP (Model Context Protocol) — remote server for Claude Cowork
mcp>=1.27.2

# Util
python-dotenv
pydantic>=2.13.4
